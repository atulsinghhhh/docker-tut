FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install --silent

# Copy source
COPY . .

# Vite dev server runs on 5173 by default; bind to 0.0.0.0 so container is accessible
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
